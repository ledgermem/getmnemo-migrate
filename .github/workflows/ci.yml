name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: ledgermem/.github/.github/workflows/node-test.yml@main
    with:
      node-version: "20"
